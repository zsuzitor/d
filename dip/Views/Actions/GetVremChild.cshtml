

@model dip.Models.ViewModel.ActionsV.GetListSomethingV<dip.Models.Domain.Vrem>

@if (Model.List != null)
{
   
    @Html.Action("DescrFormVrem", "Helpers", new { vrems = Model.List, type = Model.Type })

}

