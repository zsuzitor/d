
@model dip.Models.ViewModel.ActionsV.ChangeActionV

@*это представление сплитится по <hr />   => не менять*@
@(Model.Type)
<hr />
@Html.Action("GetFizVels","Actions",new {id=Model.FizVelId,type= Model.Type })

<hr />
@if (Model.Parametric != true)
{
    @Html.Action("GetPros", "Actions", new { id = Model.CheckboxParamsId, type = Model.Type })
}


<hr />
@if (Model.Parametric != true)
{
    @Html.Action("GetSpec", "Actions", new { id = Model.CheckboxParamsId, type = Model.Type })
}


<hr />
@if (Model.Parametric != true)
{
    @Html.Action("GetVrem", "Actions", new { id = Model.CheckboxParamsId, type = Model.Type })
}


<hr />
@if (Model.Parametric != false)
{
@Html.Action("GetParametricFizVels", "Actions", new { id = Model.ParametricFizVelsId, type = Model.Type })
}



